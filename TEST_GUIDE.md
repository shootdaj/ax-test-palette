# TEST_GUIDE.md

> This file is auto-generated by `/ax:init` and updated by `/ax:phase`.
> It maps project requirements to tests and tracks coverage.

## Stack

- **Language:** Node.js 20
- **Test framework:** vitest + supertest

## Running Tests

```bash
# Unit tests (fast, no dependencies)
npx vitest run --dir test/unit

# Integration tests
npx vitest run --dir test/integration

# Scenario tests
npx vitest run --dir test/scenarios

# Full pyramid (run all in order)
npx vitest run --dir test/unit && \
  npx vitest run --dir test/integration && \
  npx vitest run --dir test/scenarios
```

## Requirement - Test Mapping

<!-- Updated automatically after each phase -->

| Requirement | Unit Tests | Integration Tests | Scenario Tests | Status |
|---|---|---|---|---|
| _Example: User signup_ | `TestValidateEmail`, `TestHashPassword` | `TestUserRepo_Create` | `TestFullSignupFlow` | Covered |

## Phase Coverage Log

<!-- Appended after each /ax:phase run -->

### Phase N: _Title_
- **New unit tests:** 0
- **New integration tests:** 0
- **New scenario tests:** 0
- **Requirements covered:** _list_
- **Gaps:** _none or list_
